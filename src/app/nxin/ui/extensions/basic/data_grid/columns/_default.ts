import { NxDataGridColumn } from '.';

export class NxDataGridColumnImplements implements NxDataGridColumn {
    /**
     * 设置对齐方式
     * @type left | center | right
     * @default left
     */
    alignment: 'left';
}
