import { PigType } from '.';

/** 响应码枚举映射 */
export class ResponseCodeEnumMap {
    // static PigInState = PigInState;
    // static AbstractType = AbstractType;
    // static SowPigState = SowPigState;
    // static ImportField = ImportField;
    // static FatBatchEffective=FatBatchEffective;
    static PigType=PigType;
}